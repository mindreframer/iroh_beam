defmodule IrohBeam.Telemetry do
  @moduledoc """
  Optional transport telemetry boundary.

  Event metadata will use bounded-cardinality, secret-safe identifiers. Handler
  failures never affect transport correctness.
  """
end
